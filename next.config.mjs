/** @type {import('next').NextConfig} */
const nextConfig = {
  output: "export",
  basePath: "/grupo-nossa-farmacia",
  assetPrefix: "/grupo-nossa-farmacia/",
  images: {
    unoptimized: true,
  },
};

export default nextConfig;
